<h1>Editing Project</h1>


<div class="row">
  <div class="col-md-9">
    <%= image_tag(@project.picture_url, width: '100%') if @project.picture.present? %>
  </div>

  <div class="col-md-3">
    <%= render 'form' %>
    <%= link_to 'Show', @project %> |
    <%= link_to 'Back', projects_path %>
  </div>
</div>
